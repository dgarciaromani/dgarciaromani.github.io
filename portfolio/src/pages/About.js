import { Container, Typography, Box, Button, Grid, } from '@mui/material';
export default function About() {
    return (
        <Container >
            <h1>About page!</h1>
        </Container>
    );
}