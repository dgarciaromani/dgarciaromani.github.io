import { Container, Typography, Box, Button, Grid, } from '@mui/material';
export default function Contact() {
    return (
        <Container >
            <h1>Contact page!</h1>
        </Container>
    );
}